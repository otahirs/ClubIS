@page "/admin/users/settings/{userId:int}"

@attribute [Authorize]

@using ClubIS.Web.Components

@inject HttpClient Http

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-16">
    <MudText Typo="Typo.h3" GutterBottom="true">Settings</MudText>
    <MudTabs Vertical="true">
        <MudTabPanel Text="User">
            @if (SendUser != null)
            {<UserEditForm User="SendUser" CanBeRemoved="true" />}
        </MudTabPanel>
        <MudTabPanel Text="SI Cards">
            @if (SendUser != null)
            {<SICardsForm User="SendUser" />}
        </MudTabPanel>
        <MudTabPanel Text="Login">
            @if (SendUser != null)
            {<LoginForm CurrentUserId="SendUser.Id" />}
        </MudTabPanel>
        <MudTabPanel Text="Supervision">
            <MudText>Supervision</MudText>
        </MudTabPanel>
        <MudTabPanel Text="Member Fee">
            @if (SendUser != null)
            {<MemberFeeEditForm User="SendUser" />}
        </MudTabPanel>
        <MudTabPanel Text="Account State">
            @if (SendUser != null)
            {<AcountStateEditForm User="SendUser" />}
        </MudTabPanel>
    </MudTabs>
</MudContainer>

@code {
    [Parameter]
    public int? UserId { get; set; }
    private UserEditDTO SendUser;

    protected override async Task OnInitializedAsync()
    {
        if (UserId != null)
        {
            SendUser = await Http.GetFromJsonAsync<UserEditDTO>($"Users/{UserId}");
        }
    }
}
